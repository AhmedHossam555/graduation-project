import { ShowInputDirective } from './show-input.directive';

describe('ShowInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInputDirective();
    expect(directive).toBeTruthy();
  });
});
