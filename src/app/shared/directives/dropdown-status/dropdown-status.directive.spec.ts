import { DropdownStatusDirective } from './dropdown-status.directive';

describe('DropdownStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownStatusDirective();
    expect(directive).toBeTruthy();
  });
});
