import { EditButtonDirective } from './edit-button.directive';

describe('EditButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new EditButtonDirective();
    expect(directive).toBeTruthy();
  });
});
